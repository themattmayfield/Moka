const channels = [
  // { general: "894626621405921280" },
  { test: "927249226310750268" },
];

const MOKA_CHANNELS = channels.map((channel) => Object.values(channel)[0]);

export default MOKA_CHANNELS;
